let f =
  let a = 3 in
  let b = a in
  let c = b in
  c

let g z =
  let gf = fun x -> x in
  gf z

let p = g 2

typedef List a = Nil | Cons of a * List a

typedef Maybe a = Just of a | Nothing

let main = Cons(Cons(1,Nil),Nil)