typedef List a = Nil | Cons of a * List a

let rec a l = 
  match l with
    Nil -> "even"
  | Cons(x,xs) -> b xs
and b l =
  match l with
    Nil -> "odd"
  | Cons(x,xs) -> a xs

let f =
  let rec p x : List Int = Cons(1,q x)
  and q x = Cons(2,p x) in
  p

let main = a (Cons(1,Cons(2,Cons(3,Cons(4,Nil)))))